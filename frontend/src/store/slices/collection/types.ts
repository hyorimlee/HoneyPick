export interface CollectionState {
  item: {
    url: string,
    title: string,
    thumbnail: string,
    priceBefore: number,
    priceAfter: number,
    discountRate: number,
    stickers: object,
  }
}